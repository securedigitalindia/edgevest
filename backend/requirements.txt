yfinance>=0.2.38
pandas>=2.0.0
# pandas-ta: install with --no-deps on Python 3.14+ to skip numba (unsupported)
# run: grep -v pandas-ta requirements.txt | pip install -r /dev/stdin && pip install pandas-ta --no-deps
pandas-ta>=0.3.14b
requests>=2.31.0
upstox-python-sdk>=2.0.0
exchange-calendars>=4.5
flask
authlib>=1.3
python-dotenv>=1.0
flask-cors
gunicorn>=21.0
